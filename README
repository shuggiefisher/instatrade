To recreate an environment for tornado:

mkdir tornado
cd tornado
virtualenv --no-site-packages --distribute .

git clone https://github.com/shuggiefisher/instatrade.git

pip install -r instatrade/requirements.txt
